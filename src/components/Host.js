import * as React from 'react';
import Typography from '@mui/material/Typography';

const Host = () => {
    return (
        <React.Fragment>
            <Typography variant='h5' color='primary' sx={{ mb: 2 }}>
                Klaim Host
            </Typography>

            <Typography>
                Setelah bapak/ibu join
            </Typography>
        </React.Fragment>
    );
}

export default Host;